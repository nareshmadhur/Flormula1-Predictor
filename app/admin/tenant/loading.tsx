import { AdminLoadingShell } from '@/components/ui/admin-loading-shell'

export default function Loading() {
  return (
    <AdminLoadingShell
      title="Loading group admin"
      description="Loading entries, invites, bonus questions, and roster details."
    />
  )
}
